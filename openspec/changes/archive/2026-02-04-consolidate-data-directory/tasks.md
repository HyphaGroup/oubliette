# Tasks

- [x] 1. Update `cmd/server/main.go` to use `data/projects` and `data/logs` as defaults
- [x] 2. Update `config.yaml.example` paths section
- [x] 3. Update `docs/BACKUPS.md` if it references old paths
- [x] 4. Update `AGENTS.md` if it references old paths (N/A - no references)
- [x] 5. Update `.gitignore` to use `data/` instead of separate entries
- [x] 6. Build and verify: `go build ./...`
